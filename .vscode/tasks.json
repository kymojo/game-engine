// See settings.json for ${config:...}
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "SDL2 Build Debug",
            "command": "./build.ps1",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
        },
    ],
}